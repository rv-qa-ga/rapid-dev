node_modules/
out/
public/audio/*.mp3
public/audio/*.wav
public/audio/.narration-hashes.json
# Note: src/audio/durations.json is intentionally committed.
# It is regenerated by `npm run voiceover`, but it must exist on disk for the
# Remotion composition to import — without it, Studio fails to load on a fresh clone.
*.log
.DS_Store
Thumbs.db
